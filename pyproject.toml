[tool.poetry]
name = "survival-backend"
version = "0.1.0"
description = ""
authors = ["Ensign College"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.95.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
